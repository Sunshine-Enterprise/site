





<?php

/*

				$find = $_GET['find'] ?? null;

				if($find){
					$find = "%$find%";
					$query = "select jobs.*,industries.Nameindustry from jobs join industries on jobs.industry_id = industries.IndustryId where jobs.job_name like :find order by id desc limit $limit offset $offset";
					$rows = query($query,['find'=>$find]);

				}

				if($rows){
					foreach($rows as $row){
						include '../app/pages/includes/posts.php';
					}
				}else{
				?>
				<div class="alert alert-danger" role="alert">We can't find more jobs</div>
				<?php
				}

*/
				?>


